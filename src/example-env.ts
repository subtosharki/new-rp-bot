type envFile = {
    TOKEN: string;
    GUILDID: string;
    CLIENTID: string;
    UNB_TOKEN: string;
};

const env: envFile = {
    TOKEN: '',
    GUILDID: '',
    CLIENTID: '',
    UNB_TOKEN: '',
};

export default env;
