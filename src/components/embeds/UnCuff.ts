import { MessageEmbed } from "discord.js";

export const unCuffing: MessageEmbed = new MessageEmbed()
    .setColor('#004cff')
    .setTimestamp()
    .setDescription(`Taking off cuffs...`);

export const unCuffed: MessageEmbed = new MessageEmbed()
    .setColor('#004cff')
    .setTimestamp();